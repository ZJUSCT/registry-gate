apiVersion: v2
name: registry-gate
description: |
  Authz gate in front of a Docker Registry v2 token endpoint (validated
  against Harbor pull-through cache): anonymous pulls limited to a
  whitelist, PAT holders pull everything, admin panel for tokens.
type: application
# Placeholders: the release workflow (release.yml) stamps both fields from
# the release tag; keep the lines intact so the sed patterns still match.
version: 0.0.0
appVersion: "0.0.0"
